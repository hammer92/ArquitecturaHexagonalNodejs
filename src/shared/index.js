const infrastructure = require('./infrastructure')
const application = require('./application')
const domain = require('./domain')
const utils = require('./utils')

module.exports = {
    infrastructure, application, domain, utils
}