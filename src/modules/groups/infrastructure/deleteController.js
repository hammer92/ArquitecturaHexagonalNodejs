// const { User } = require('../../loaders/sequelize');
module.exports = {
    async invoke (req, res) {
        try {

            res.json()
        }
        catch (e) {
            // errorSerializer.json(e, res)
        }
    }
}
